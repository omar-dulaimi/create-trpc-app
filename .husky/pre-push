#!/usr/bin/env sh
. "$(dirname "$0")/_/husky.sh"

# Lint and format check before push
npm run format:check || { echo "Prettier check failed"; exit 1; }
npm run lint || { echo "ESLint failed"; exit 1; }
